import { css } from '@emotion/react';

import { COMMON_SIZE } from 'styles/sizes';

export const mainWrapperStyle = css({
  paddingLeft: COMMON_SIZE.COMMON_PAGE_PADDING_HORIZONTAL,
  paddingRight: COMMON_SIZE.COMMON_PAGE_PADDING_HORIZONTAL,
});
